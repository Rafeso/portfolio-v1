---
import Link from "./Link.astro";

import navData from "../data/navData";
import { cn } from "@utils/lib";
---

<header>
  <nav class="fixed inset-x-0 top-0 z-10 backdrop-blur backdrop-opacity-80">
    <div
      class="container flex text-zinc-300 w-full justify-center items-center mx-auto p-6"
    >
      <ul>
        <li class="flex gap-2 text-sm items-center">
          <Link
            text="Página Inicial"
            href="/"
            classes={cn(
              "duration-500 hover:text-zinc-50",
              Astro.url.pathname === "/" && "text-zinc-50",
            )}
          />
          <div class="h-4 w-[1px] bg-zinc-500"></div>
          <Link
            text="Meus Projetos"
            href="/projects"
            classes={cn(
              "duration-500 hover:text-zinc-50",
              Astro.url.pathname.startsWith("/projects") && "text-zinc-50",
            )}
          />
        </li>
      </ul>
    </div>
  </nav>
</header>
